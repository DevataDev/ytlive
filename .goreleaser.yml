version: 2
before:
  hooks:
    - go generate
    - go mod download

builds:
  - id: ytlive
    env:
      - CGO_ENABLED=1
    goos:
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
      - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
      - "-X windsorf-youtube-live/internal/version.Date={{.Date}}"
    overrides:
      - goos: windows
        goarch: amd64
        flags: 
          - -trimpath
        env:
          - CC=zig cc -target x86_64-windows-gnu
          - CXX=zig c++ -target x86_64-windows-gnu
          - CGO_ENABLED=1
        ldflags:
          - -s -w
          - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
          - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
          - "-X windsorf-youtube-live/internal/version.Date={{.Date}}"

      - goos: windows
        goarch: 'arm64'
        flags: 
          - -trimpath
        env:
          - CGO_ENABLED=1
          - CC=zig cc -target aarch64-windows-gnu
          - CXX=zig c++ -target aarch64-windows-gnu
        ldflags:
          - -s -w
          - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
          - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
          - "-X windsorf-youtube-live/internal/version.Date={{.Date}}"

      - goos: linux
        goarch: amd64
        flags: 
          - -trimpath
        env:
          - CC=zig cc -target x86_64-linux-musl
          - CXX=zig c++ -target x86_64-linux-musl
          - CGO_ENABLED=1
        ldflags:
          - -s -w
          - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
          - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
          - "-X windsorf-youtube-live/internal/version.Date={{.Date}}"
      
      - goos: linux
        goarch: arm64
        flags: 
          - -trimpath
        env:
          - CC=zig cc -target aarch64-linux-musl
          - CXX=zig c++ -target aarch64-linux-musl
          - CGO_ENABLED=1
        ldflags:
          - -s -w
          - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
          - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
          - "-X windsorf-youtube-live/internal/version.Date={{.Date}}"
      
      - goos: linux
        goarch: mips64
        flags: 
          - -trimpath
        env:
          - CC=zig cc -target mips64-linux-musl
          - CXX=zig c++ -target mips64-linux-musl
          - CGO_ENABLED=1
        ldflags:
          - -s -w
          - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
          - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
          - "-X windsorf-youtube-live/internal/version.Date={{.Date}}"

      
      - goos: darwin
        goarch: arm64
        flags: 
          - -trimpath
          - -buildmode=pie
        env:
          - CGO_ENABLED=1
          - CC=zig cc -target aarch64-macos-none -F{{ .Env.SDK_PATH }}/System/Library/Frameworks
          - CXX=zig c++ -target aarch64-macos-none -F{{ .Env.SDK_PATH }}/System/Library/Frameworks
        ldflags:
          - -s -w
          - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
          - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
          - "-X windsorf-youtube-live/internal/version.Date={{.Date}}"
      
      - goos: darwin
        goarch: amd64
        flags: 
          - -trimpath
          - -buildmode=pie
        env:
          - CGO_ENABLED=1
          - CC=zig cc -target x86_64-macos-none -F{{ .Env.SDK_PATH }}/System/Library/Frameworks
          - CXX=zig c++ -target x86_64-macos-none -F{{ .Env.SDK_PATH }}/System/Library/Frameworks
        ldflags:
          - -s -w
          - "-X windsorf-youtube-live/internal/version.Version={{.Version}}"
          - "-X windsorf-youtube-live/internal/version.Commit={{.Commit}}"
          - "-X windsorf-youtube-live/internal/version.Date={{.Date}}" 

changelog:
  sort: asc
  use: github
  format: "{{ .SHA }}: {{ .Message }}"
  filters:
    exclude:
      - "^test:"
      - "^test\\("
      - "^chore: update$"
      - "^chore: docs$"
      - "^docs: update$"
      - "^chore: schema$"
      - "^chore: typo$"
      - "^chore: auto-update generated files$"
      - "^chore: update schema$"
      - "^chore: schema update$"
      - "^chore\\(deps\\): "
      - "^(build|ci): "
      - "merge conflict"
      - "merge conflict"
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch
      - go mod tidy
  groups:
    - title: "New Features"
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 100
    - title: "Security updates"
      regexp: '^.*?sec(\(.+\))??!?:.+$'
      order: 150
    - title: "Bug fixes"
      regexp: '^.*?(fix|refactor)(\(.+\))??!?:.+$'
      order: 200
    - title: "Documentation updates"
      regexp: ^.*?docs?(\(.+\))??!?:.+$
      order: 400
    - title: Other work
      order: 9999

archives:
  - formats: ['tar.gz']
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - web/**

nfpms:
  - id: ytlive
    package_name: ytlive
    file_name_template: "{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    vendor: YukLive
    homepage: https://yuklive.my.id
    maintainer: YukLive <yuklive@yuklive.my.id>
    description: YukLive is an utility to easier process TikTok and YouTube live streaming.
    license: MIT
    section: utils
    priority: extra
    ids:
      - ytlive
    contents:
      # Include the built web assets
      - src: web/**
        dst: /opt/ytlive/
        type: dir 
        file_info:
          mode: 0644
          mtime: "{{.CommitDate}}"
      
      # Include the binary
      - src: "{{ .ArtifactPath }}"
        dst: /opt/ytlive/{{ .ProjectName }}
        type: file
        file_info:
          mode: 0755
          mtime: "{{.CommitDate}}"
      
      # Create a symlink for easier access
      - src: /opt/ytlive/{{ .ProjectName }}
        dst: /usr/bin/{{ .ProjectName }}
        type: symlink

    formats:
      - deb
      - rpm